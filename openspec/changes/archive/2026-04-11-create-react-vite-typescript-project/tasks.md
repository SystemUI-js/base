## 1. 初始化工程骨架

- [x] 1.1 在仓库根目录创建 `package.json`，添加 React、Vite、TypeScript 所需依赖并定义 `dev`、`build` 脚本
- [x] 1.2 创建 Vite、TypeScript 与基础 HTML 入口配置文件，确保项目具备开发与生产构建能力
- [x] 1.3 建立 `src/main.tsx`、`src/App.tsx`、基础样式与资源目录，形成可扩展的最小应用结构

## 2. 集成代码质量工具

- [x] 2.1 配置适用于 React + TypeScript 的 ESLint 规则与忽略项，并接入 `lint` 脚本
- [x] 2.2 配置 Prettier 及其忽略项，处理与 ESLint 的规则边界，并接入 `format` 脚本
- [x] 2.3 校准脚手架示例代码与配置文件，确保初始状态下格式化与静态检查不会互相冲突

## 3. 完成示例页面与验收

- [x] 3.1 实现极简示例页面，验证根组件挂载、样式加载与 TypeScript 编译链路正常
- [x] 3.2 执行并确认 `dev`、`build`、`lint`、`format` 脚本可用，修正初始化过程中暴露的基础问题
