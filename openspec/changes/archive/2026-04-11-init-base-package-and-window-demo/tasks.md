## 1. 包元数据与导出面整理

- [x] 1.1 调整根目录 `package.json`，补齐 `@system-ui-js/base` 的包名、`exports`、类型入口、`files` 范围以及独立的库构建与 Demo 构建脚本。
- [x] 1.2 增加基础包公开入口与必要的适配层，明确对外导出面，并梳理 `react`、`react-dom` 与 `@system-ui-js/chameleon` 的依赖边界。

## 2. 双目标构建链路落地

- [x] 2.1 为基础包构建与 Demo 构建拆分 Vite / TypeScript 配置，确保两类产物可以独立执行且输出到不同目录。
- [x] 2.2 校准构建产物目录、声明文件输出与包入口映射，避免发布内容混入 Demo 静态资源。

## 3. Window Demo 场景实现

- [x] 3.1 接入 `@system-ui-js/chameleon`，通过 `@system-ui-js/base` 的公共导出面封装并暴露首批 Window 场景所需能力。
- [x] 3.2 替换当前占位页面，完成 Window 首屏展示与基础交互反馈，并确保 Demo 页面优先消费公共导出而非深层实现文件。
- [x] 3.3 拆分基础包运行必需样式与 Demo 专属展示样式，保持库样式可复用、Demo 样式不外泄。

## 4. 交付验证与收尾

- [x] 4.1 验证基础包单独构建可生成可发布的 JavaScript 与类型声明产物，且不依赖 Demo 构建前置。
- [x] 4.2 验证 Demo 单独构建可生成可部署站点产物，并补充聚合构建流程以覆盖双目标交付场景。
