## 1. 精简默认 Demo 入口

- [x] 1.1 重构 `src/App.tsx`，移除多窗口、模式切换、摘要指标、日志刷新等状态与面板逻辑，只保留 `BaseThemeProvider` 包裹的单个 Win98 主题窗口。
- [x] 1.2 将首页窗口内容改为静态说明文案与简短列表，并继续仅通过 `@system-ui-js/base` 的公开导出渲染标题栏与内容区。

## 2. 收敛单窗口展示样式

- [x] 2.1 重写 `src/styles/app.css` 中围绕工作台、辅助窗口、指标卡片和日志区域的样式，删除与单窗口方案无关的选择器。
- [x] 2.2 补齐单窗口居中布局、内容区排版层级与窄视口下的宽度/滚动约束，确保首屏可读且不溢出。

## 3. 验证默认首页展示

- [x] 3.1 运行 Demo 相关验证命令，确认默认入口仍能通过 `@system-ui-js/base` 公共 API 正常渲染 Win98 主题窗口。
- [x] 3.2 检查默认首页仅展示单一窗口主体，且不再出现辅助窗口、模式切换、摘要指标或日志面板等旧工作台元素。
